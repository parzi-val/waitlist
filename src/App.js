import React from 'react';
import AppContainer from './components/AppContainer'; // Adjust the path as per your project structure
import './App.css';
import './theme.scss';
import './assets/css/materialdesignicons.min.css';

function App() {
  return (
    <AppContainer />
  );
}

export default App; // Ensure to export the component as default
